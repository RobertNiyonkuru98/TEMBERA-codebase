generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model TestUser {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
